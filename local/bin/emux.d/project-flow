# Create a series of new windows (some named)
new-window -n "git"
new-window -n "build"
new-window -n "tests"
new-window
new-window
new-window -n "search&docs"

# Select the first window
select-window -t 0
